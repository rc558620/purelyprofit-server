- 采用 Provider-Registry 模式解耦缓存操作：将特定业务的缓存键处理逻辑封装为 Provider，再通过 Registry 工厂函数聚合，避免服务类膨胀。
- 缓存失效方法命名遵循 `invalidate[Domain][Feature]` 规范，并统一接收 `storeId` 或 `userId` 作为定位参数。
- 预热与失效逻辑均按业务域（如 finance, profit-read, pulse）拆分为独立的 Service 或 Config 集合，保持单一职责。