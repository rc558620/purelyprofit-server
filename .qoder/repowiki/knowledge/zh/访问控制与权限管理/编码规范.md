- 权限校验优先使用通配符 '*' 匹配，若未命中则精确匹配权限代码字符串。
- 子账号（sub_account）的权限与功能模块可见性严格受限于其角色预设集合（如 cashier, finance, manager）及激活状态。
- 使用 Reflector 从装饰器中提取元数据（如 REQUIRE_PERMISSIONS_KEY），在 Guard 执行阶段动态获取接口所需的权限要求。
- 身份上下文（AuthenticatedMembership）统一封装员工ID、门店ID、角色类型及子账号关联信息，作为权限计算的唯一输入源。