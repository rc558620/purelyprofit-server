- 采用“声明式元数据 + 守卫执行”模式，通过 SetMetadata 定义权限需求，由 Guard 在运行时统一解析校验。
- 权限判定支持通配符 '*' 逻辑，当用户拥有通配符权限时直接放行所有细粒度权限检查。
- 子账号权限采用“白名单预定义”策略，不同子账号角色（如收银员、财务）对应固定的权限集合与首页模块可见性。
- 身份上下文统一抽象为 AuthenticatedMembership 接口，屏蔽 Staff 与 SubAccount 在底层数据模型上的差异。