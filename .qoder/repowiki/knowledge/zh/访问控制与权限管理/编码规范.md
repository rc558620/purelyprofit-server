- 使用 SetMetadata 定义元数据键，并通过 applyDecorators 封装业务语义化的装饰器（如 RequirePermissions）。
- 守卫（Guard）通过 Reflector 读取装饰器元数据，并结合 Request 上下文中的 user 信息进行逻辑判断。
- 权限判定支持通配符 '*' 逻辑，在 AccessControlService 中优先检查通配符以简化超级管理员的权限校验。
- 子账号权限采用“白名单+状态校验”模式，仅在账号激活且已分配时返回特定角色预设的权限数组。