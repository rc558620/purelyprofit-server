- 所有 Redis 操作均通过 `observeRedisCommand` 包装，自动记录耗时并根据配置的阈值（默认 20ms）输出慢查询警告。
- 缓存键采用冒号分隔的层级结构（如 `profit:dashboard:home:store:{id}`），并为每个业务域配套提供精确匹配与通配符模式构建函数。
- 批量失效操作优先使用 `delByPattern` 结合 `SCAN` 命令，避免在生产环境中直接使用阻塞式的 `KEYS` 指令。