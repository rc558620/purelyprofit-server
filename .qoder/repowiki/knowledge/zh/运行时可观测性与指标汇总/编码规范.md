- 所有数值型指标在存入状态或生成快照前，均通过 `roundMetric` 工具函数保留两位小数以确保精度一致。
- 慢请求/慢查询等采样数据通过 `pushCappedItem` 工具函数进行 capped array 管理，默认限制为 20 条以防止内存无限增长。
- 针对 SQL 查询语句，在记录前统一调用 `normalizeQueryWhitespace` 进行空白符标准化处理，以便于后续按操作类型聚合统计。