- 所有数值型指标在存入状态或生成快照前，统一通过 `roundMetric` 工具函数保留两位小数，确保数据精度一致。
- 对于慢请求、慢查询等采样数据，统一使用 `pushCappedItem` 进行定长数组管理，超出上限时自动丢弃最旧记录以控制内存占用。
- 在生成路由或命令维度的统计快照时，统一先计算平均值与错误率，再按总耗时降序排列并截取 Top N（如 Top 20），以突出性能瓶颈。