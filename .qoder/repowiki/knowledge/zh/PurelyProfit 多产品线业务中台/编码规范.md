- 采用领域驱动设计（DDD）分层，业务逻辑按产品线（purely-profit/club/pulse）物理隔离，共享能力下沉至 shared 目录。
- 统一使用 `@nestjs/config` 管理环境变量，并通过 `ConfigService` 在模块工厂中异步注入配置。
- 遵循 RESTful 规范，控制器层仅负责参数校验与响应封装，核心业务逻辑委托给以 `.service.ts` 结尾的领域服务。