- 所有业务模块遵循 Controller-Service-DTO 分层架构，且 Service 层负责核心业务逻辑与事务编排。
- 数据库模型按业务领域（如 finance, member, marketing）拆分定义在 prisma/purely-profit 子目录下，保持 schema 的模块化。
- 缓存管理采用统一的 Provider 模式，将缓存失效（Invalidator）与预热（Prewarm）逻辑从业务服务中解耦，并通过 RedisModule 集中注册。