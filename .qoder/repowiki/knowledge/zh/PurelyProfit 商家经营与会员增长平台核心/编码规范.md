- 所有子模块均通过 NestJS 的 `Module` 装饰器进行封装，并在根模块 `AppModule` 中显式声明依赖关系。
- 全局采用 `ValidationPipe` 并开启 `whitelist` 和 `forbidNonWhitelisted`，强制要求所有 DTO 严格定义输入字段。
- 通过 `src/config/configuration.ts` 统一管理所有环境变量的解析与默认值，禁止在业务代码中直接读取 `process.env`。