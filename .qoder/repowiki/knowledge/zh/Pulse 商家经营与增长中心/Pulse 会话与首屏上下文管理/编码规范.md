- 服务层方法统一接收 AuthenticatedUser 对象而非原始 token，并在内部通过依赖服务解析业务上下文。
- DTO 转换逻辑抽取至独立的 utils 文件（如 buildUserDto, buildStoreDto），保持 Service 层专注于业务流程。
- 缓存键构建与失效逻辑通过专用的 cache-keys 模块和 CacheInvalidatorService 集中管理，避免硬编码。
- 所有对外暴露的 API 响应均通过 Swagger 装饰器（@ApiProperty）定义详细的元数据和校验规则（class-validator）。