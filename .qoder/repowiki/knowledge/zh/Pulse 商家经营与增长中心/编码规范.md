- 所有业务子模块均采用 `*.module.ts` 定义模块边界，并通过 `imports` 显式声明对 `PulseStoreContextModule` 或 `AuthModule` 的依赖。
- 控制器与服务命名遵循 `Pulse[Domain][Feature]Controller/Service` 格式（如 `PulseDashboardController`），以保持 Pulse 域内标识的唯一性。
- DTO 定义统一存放在各子模块的 `dto/` 目录下，并按功能细分（如 `pulse-dashboard-query.dto.ts`），实现请求与响应结构的类型安全。