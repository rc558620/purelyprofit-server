- 所有对外接口均通过 `PulseJwtAuthGuard` 保护，并使用 `@CurrentUser` 装饰器提取认证用户信息。
- 管理端列表查询普遍采用基于游标的分页机制，并在 DTO 中定义默认的 `limit` 与最大限制。
- 涉及敏感操作（如申请合伙人、修改提现账户）在开发者观察态下直接抛出 `ForbiddenException` 以禁止执行。
- 数据访问层通过 `PulseGrowthAccessService` 动态构建 Prisma `WhereInput`，实现开发者账号与真实商家数据的物理隔离。