- 核心服务 `PulseStoreContextService` 封装了门店解析逻辑，优先使用请求参数，其次回退到 Redis 缓存的用户偏好。
- 通过 `PULSE_TARGET_STORE_SELECT` 统一定义 Prisma 查询投影，确保数据结构一致性。
- 依赖 `PrismaService` 进行数据持久层访问，`RedisService` 处理用户会话级的门店选择状态。
- 模块导出服务供其他业务模块（如报表、库存）注入使用，实现上下文共享。