- 所有业务模型均遵循多租户隔离设计，核心实体（如 Member, FinanceAccountRecord）强制包含 `storeId` 字段并建立关联索引。
- Schema 定义中广泛使用复合索引（@@index）优化常见查询路径，特别是针对 `storeId`、`status` 和 `updatedAt` 的组合过滤场景。
- 在迁移脚本中引入 PostgreSQL 扩展（如 `pg_trgm`）以支持特定字段的模糊搜索性能优化，体现对数据库原生能力的深度利用。