- 所有业务模型表名采用复数形式蛇形命名（如 `finance_account_records`），并通过 `@@map` 显式指定。
- 多租户隔离模式下，绝大多数业务表包含 `storeId` 字段并建立以 `storeId` 为前缀的复合索引以优化查询。
- 审计字段统一包含 `createdAt` (默认 now) 和 `updatedAt` (自动更新)，并映射为蛇形命名的列。
- 枚举类型定义在对应的领域模型文件中，采用 PascalCase 命名，并在数据库中存储为小写字符串或整数。