- 所有业务模型均包含 `storeId` 字段并建立关联，强制实现多租户数据隔离。
- 模型命名遵循 `{Domain}{Entity}` 格式（如 `FinanceAccountRecord`, `MemberPointsLog`），并在 `@@map` 中指定蛇形命名的物理表名。
- 为支持审计与追踪，核心业务模型普遍包含 `operatorStaffId` 关联字段及 `createdAt`/`updatedAt` 时间戳。
- 金额字段统一使用 `Decimal @db.Decimal(12, 2)` 类型以确保财务计算的精度。