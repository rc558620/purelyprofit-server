- 所有业务模型均包含 `storeId` 字段并建立索引，强制实现多租户数据隔离。
- 模型普遍采用 `createdAt` 和 `updatedAt` 字段，并利用 Prisma 的 `@default(now())` 和 `@updatedAt` 属性自动维护时间戳。
- 针对高频查询场景（如列表过滤、状态筛选），在模型定义中显式声明复合索引（`@@index`）以优化数据库性能。
- 枚举类型（Enum）命名采用 `PascalCase` 且具备明确的业务前缀（如 `MemberStatus`, `FinanceAccountType`），避免全局命名冲突。