- 所有业务模型文件均按领域归类存放于 `prisma/purely-profit` 的子目录中，保持 schema.prisma 的简洁性。
- 模型字段命名统一采用 camelCase，并通过 `@map` 属性显式映射为数据库的 snake_case 列名。
- 关键业务表普遍定义复合索引以优化多条件查询性能，索引命名遵循 `表名_字段组合_idx` 或 Prisma 默认规范。
- 涉及金额或精度的字段统一使用 `Decimal` 类型并指定精度（如 `@db.Decimal(12, 2)`），避免浮点数误差。