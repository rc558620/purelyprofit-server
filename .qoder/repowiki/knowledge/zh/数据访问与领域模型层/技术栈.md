- **ORM**: Prisma Client JS with `PrismaPg` adapter.
- **Database**: PostgreSQL (utilizing `pg` driver and `pg_trgm` extension for GIN indexes).
- **Framework Integration**: NestJS (Global Module, Dependency Injection).