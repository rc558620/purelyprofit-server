- 服务层拆分为职责单一的子服务，如访问控制（AccessService）与业务数据处理（ProfileService）。
- 数据库查询逻辑独立封装在 `.query.ts` 文件中，避免在服务类中直接编写 Prisma 调用。
- DTO 继承基础类以复用验证规则，如 `UpdateMonthlyMembershipSettingDto` 继承 `UpdateMembershipPriceDto`。
- 使用常量文件（`.constants.ts`）管理默认配置与枚举顺序，确保业务规则集中维护。