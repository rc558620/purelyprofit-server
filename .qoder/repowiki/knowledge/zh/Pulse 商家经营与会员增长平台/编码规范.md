- 所有业务子模块均采用 `*.module.ts` 进行依赖注入管理，并遵循 NestJS 标准分层结构。
- 核心业务模块（如 dashboard, growth, membership）均统一导入 `PulseStoreContextModule` 以获取门店上下文。
- DTO 定义统一放置在 `dto/` 子目录下，并按功能域拆分为 request/response/shared 类型文件。