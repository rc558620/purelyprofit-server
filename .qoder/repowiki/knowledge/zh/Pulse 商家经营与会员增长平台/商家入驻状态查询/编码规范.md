- 服务层拆分为门面服务（OnboardingService）与具体业务逻辑服务（OnboardingStatusService），以实现职责分离。
- DTO 设计中保留 deprecated 的旧字段（如 isCompleted, steps）以维持向后兼容，同时引入新的结构化字段（targetStatus）。
- 利用工具函数（onboarding.utils.ts）封装通用的业务判断逻辑（如 isActiveMembership），保持服务代码简洁。