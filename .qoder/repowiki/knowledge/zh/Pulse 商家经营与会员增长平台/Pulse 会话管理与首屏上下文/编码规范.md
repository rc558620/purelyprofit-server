- 会话敏感数据（如 bootstrap 响应和通知计数）采用短 TTL（15秒）Redis 缓存策略，并在状态变更（如切换门店）时主动失效相关缓存。
- DTO 定义与业务逻辑解耦，通过 `session.utils.ts` 中的纯函数将数据库实体或内部类型转换为 Swagger 装饰的 DTO 对象。
- 服务类遵循单一职责原则，将复杂的聚合逻辑（bootstrap）、状态变更（switch store）和子领域计算（notifications）拆分为独立的服务类。