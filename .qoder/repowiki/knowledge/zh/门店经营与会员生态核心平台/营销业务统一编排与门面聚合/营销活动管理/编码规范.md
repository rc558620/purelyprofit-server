- 所有写操作（创建、更新、删除）后均调用 `invalidateDashboardCaches` 确保仪表盘数据一致性。
- 在 Service 层统一使用 `MarketingSharedService` 进行店铺归属权校验和用户权限断言，而非在 Controller 层处理。
- DTO 中的字符串字段在入库前统一执行 `.trim()` 处理，且时间范围在写入前进行合法性校验（结束时间必须晚于开始时间）。