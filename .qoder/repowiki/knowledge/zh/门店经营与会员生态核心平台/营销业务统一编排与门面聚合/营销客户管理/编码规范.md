- 所有写操作（创建、更新、删除）后均调用`invalidateOverviewCache`清除相关门店的营销概览缓存，确保数据实时性。
- 涉及敏感数据（如手机号）的唯一性校验在Service层通过`ensureUniquePhone`私有方法统一处理，并在冲突时抛出`ConflictException`。
- DTO转换与数据映射统一使用`mapCustomerRow`等映射函数，保持API响应结构的一致性。
- 业务逻辑中广泛使用`marketingSharedService`进行跨模块的通用校验（如门店访问权限、顾客存在性检查），避免逻辑重复。