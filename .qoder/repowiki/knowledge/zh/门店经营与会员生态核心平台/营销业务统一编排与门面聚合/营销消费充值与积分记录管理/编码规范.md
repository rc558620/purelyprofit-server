- 所有列表查询均采用“并行执行”模式，同时发起数据查询与 count 统计以提升响应速度。
- 数据库查询层统一使用原生 SQL 模板字符串，并通过 `Prisma.sql` 动态拼接可选过滤条件。
- 写操作后必须重新查询单条记录并映射返回，以确保返回数据包含最新的关联信息（如顾客姓名）。
- 涉及金额变动的操作必须在数据库事务中完成，并同步更新客户表的冗余字段（如 totalSpent, visitCount）。