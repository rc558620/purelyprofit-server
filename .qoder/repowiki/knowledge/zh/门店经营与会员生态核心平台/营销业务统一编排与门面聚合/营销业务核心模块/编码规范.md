- 金额字段在 API 响应中统一使用“分”为单位的整数，时间戳统一使用毫秒级整数。
- 敏感信息（如手机号）在 Mapper 层进行脱敏处理，原始数据不直接暴露给前端。
- 动态状态（如顾客活跃状态、活动进行中状态）不在数据库中持久化，而是根据时间戳在查询时实时计算。
- 分页逻辑封装在 `marketing.utils.ts` 的 `resolveMarketingPagination` 中，统一处理页码与每页数量的边界情况。