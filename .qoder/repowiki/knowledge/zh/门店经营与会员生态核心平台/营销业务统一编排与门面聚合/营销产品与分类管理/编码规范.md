- 所有写操作（创建、更新、删除、切换状态）前均通过 `MarketingSharedService` 执行门店级别的权限校验（`ensureMarketingStoreAccess`）。
- 在创建或更新产品分类时，强制校验同一门店下分类名称的唯一性，冲突时抛出 `ConflictException`。
- 删除产品分类前必须检查是否存在关联的营销产品，若存在则抛出 `BadRequestException` 阻止删除以维护引用完整性。
- DTO 中的字符串字段（如名称、描述）在持久化前统一执行 `.trim()` 处理，并使用工具函数（如 `toNullableText`）规范化空值。