- DTO 层统一使用 `class-validator` 装饰器进行请求参数校验，并结合 `ApiProperty` 生成 Swagger 文档。
- 服务层方法优先通过 `getOrLoadRefreshableJson` 模式读取缓存，仅在缓存缺失或过期时触发数据库查询与计算。
- 金额处理统一调用 `finance-money.utils` 中的工具函数（如 `toMoneyNumber`, `roundMoneyValue`）以确保精度一致性与前端展示兼容。
- 数据访问逻辑抽离至独立的 `.query.ts` 文件，保持 Service 类专注于业务流程编排与异常处理。