- 采用门面模式（Facade），由 `FinanceController` 统一接收请求并委托给 `FinanceService`，后者进一步分发至各子领域服务（CashFlow, Account, Reconciliation, Overview）。
- 通过 `FinanceAccessService` 实现跨子模块的统一权限校验，确保所有财务操作均绑定当前用户的门店上下文并验证功能开通状态。
- 共享 `finance.mapper.ts` 及分页工具类，为不同子模块的列表查询提供标准化的 DTO 转换与分页元数据构建逻辑。
- 依赖 `AuthModule` 与 `PlatformMembershipModule` 实现身份认证与会员体系集成，确保财务数据隔离在门店维度。