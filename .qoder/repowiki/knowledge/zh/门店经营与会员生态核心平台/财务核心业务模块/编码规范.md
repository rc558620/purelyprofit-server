- 所有子模块服务均通过统一的 `FinanceService` 门面进行暴露，而非直接在 Controller 中注入多个子服务。
- 列表查询响应均采用统一的 `buildPaginated...Response` 工具函数构建，确保分页结构一致性。
- 所有业务方法均强制注入 `AuthenticatedUser` 并通过 `FinanceAccessService` 提取 `storeId` 以实现数据隔离。