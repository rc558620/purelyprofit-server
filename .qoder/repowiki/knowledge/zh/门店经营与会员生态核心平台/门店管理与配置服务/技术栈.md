- **数据存储**：使用 Prisma ORM 操作关系型数据库存储门店核心记录。
- **缓存机制**：利用 Redis (`RedisService`) 存储门店扩展元数据 (`stores:profile:{id}`)，提升读取性能并支持非结构化属性。
- **验证库**：采用 `class-validator` 和 `class-transformer` 进行 DTO 校验与数据转换（如 `transformOptionalInt`）。