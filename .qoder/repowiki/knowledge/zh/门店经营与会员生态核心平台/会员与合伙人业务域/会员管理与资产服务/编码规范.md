- 所有写操作（创建、更新、删除、资产调整）完成后，必须调用 `cacheInvalidatorService.invalidateMembersDerived` 清除关联缓存。
- 列表与概览接口优先从 Redis 缓存读取，缓存未命中或过期时触发数据库查询并回写缓存。
- 涉及敏感数据变更（如手机号唯一性校验、资产扣减）的操作必须包裹在 `prisma.$transaction` 事务中执行。
- 控制器层统一使用 `@RequirePermissions` 装饰器配合 `JwtAuthGuard` 和 `PermissionsGuard` 进行细粒度权限控制。