- 各子模块通过 NestJS Module 机制独立封装，统一依赖 `AuthModule` 实现跨模块的身份鉴权与权限隔离。
- `members` 与 `platform-membership` 模块采用 `forwardRef` 处理循环依赖，确保在复杂的会员-合伙人关联场景下服务调用的稳定性。
- 核心业务服务（如 `MembersService`, `WithdrawalsService`, `PlatformMembershipService`）通过 `exports` 暴露接口，支持上层应用或其他业务域进行跨模块组合调用。