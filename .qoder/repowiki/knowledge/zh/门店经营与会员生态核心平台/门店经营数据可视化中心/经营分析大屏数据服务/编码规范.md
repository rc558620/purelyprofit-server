- DTO 类统一使用 `class-transformer` 的 `@Type` 和 `@Transform` 装饰器处理入参类型转换与标准化（如兼容旧版 `period: 'all'`）。
- 领域聚合函数（如 `buildSalesAggregation`）返回包含 `Map` 结构的中间态结果，由 Mapper 层统一转换为排序后的数组 DTO。
- 查询工具函数（`resolveCurrentRange`）集中处理时间周期逻辑，支持预设周期（today/week/month 等）与自定义时间戳范围的自动适配。