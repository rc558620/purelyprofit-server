- 所有业务模块均遵循“控制器-服务-仓储/查询”的分层架构，并通过独立的 `*.module.ts` 文件显式声明依赖关系。
- 跨模块的敏感数据访问（如财务、会员资产）均强制引入对应的 `*AccessService` 或全局 `PermissionsGuard` 进行前置权限校验。
- 复杂业务域（如 Marketing, Operations）普遍采用 Facade（门面）模式，通过 `*FacadeService` 聚合多个子服务，对外提供简化的统一接口。