- 均采用 CQRS 思想拆分读写服务（如 ProfileRead/ProfileWrite），以优化复杂查询与事务更新的职责边界。
- 统一使用 `EmployeesAccessService` 或 `StaffAccessService` 封装门店维度的权限校验逻辑，避免控制器层出现重复的鉴权代码。
- 所有对外接口均通过 DTO 进行参数约束，并利用 Swagger 装饰器（如 `@ApiTags`）保持 API 文档的一致性。