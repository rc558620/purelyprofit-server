- 所有涉及金额的加减乘除及百分比计算均通过 `Decimal` 实例操作并统一保留两位小数（ROUND_HALF_UP）。
- 分页参数解析遵循“页码默认为1、 pageSize 限制最大值”的安全兜底策略，并返回包含 skip/take 的标准结构。
- 时间范围工具函数统一返回 Date 对象或毫秒时间戳，并提供获取日/周/月/季度起始时间的标准化方法。
- 字符串处理工具区分“可选文本”（空值转 undefined）与“可空文本”（空值转 null），并对媒体文本进行 blob 协议过滤。