- 子模块通过 NestJS 依赖注入形成单向依赖链：Inventory 依赖 Products 以联动商品生命周期，Products 与 Categories 均依赖 CommerceModule 进行门店权限与会员配额校验。
- 共享 PrismaModule 确保在事务边界内（如库存调整、商品创建）维持数据原子性。
- 统一接入 JwtAuthGuard 与 PermissionsGuard，基于 `goods:*` 与 `inventory:*` 等细粒度权限标识实现接口级访问控制。