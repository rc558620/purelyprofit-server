- 所有涉及金额或利润的字段在 Service 层均通过 validateMoneyFields 进行非负及有效性校验。
- 商品分类（Category）采用“先查后建”策略，若分类不存在则自动创建，确保数据一致性。
- 商品编号（Code）支持手动指定或自动生成，生成时采用重试机制以避免并发冲突。
- 数据访问层统一使用 select 投影对象（productSelect）以精确控制返回字段，避免敏感或冗余数据泄露。