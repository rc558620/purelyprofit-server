- 权限校验支持通配符 '*'，拥有通配符权限的主体被视为拥有所有权限。
- 子账号的接口权限与首页模块可见性严格绑定，若 `canAccessHome` 为 false 则回收所有权限。
- 使用 `SetMetadata` 自定义装饰器在控制器或方法级别声明权限需求或访问限制。
- 权限代码采用 `resource:action` 的命名格式（如 `goods:view`, `staff:create`）。