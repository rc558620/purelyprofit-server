- 采用虚拟邮箱格式（如 `profit_phone_{phone}@purelyprofit.local`）作为系统内部统一的用户唯一标识，以兼容手机号与自定义账号登录。
- 在密码修改或重置成功后，通过递增 Redis 中的 `sessionVersion` 使旧令牌在后续鉴权时因版本不匹配而自动失效。
- 通过配置化的开发者邮箱白名单（`pulse.devAccountEmails`）及特定手机号动态判定 `developer` 身份，从而授予跨产品线的访问权限。