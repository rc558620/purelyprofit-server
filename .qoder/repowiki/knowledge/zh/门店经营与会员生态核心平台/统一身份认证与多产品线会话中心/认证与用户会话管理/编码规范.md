- 所有需要身份认证的接口均统一使用 `@UseGuards(JwtAuthGuard)` 和 `@ApiBearerAuth()` 装饰器组合，确保权限控制与文档标注的一致性。
- 在控制器方法中，通过 `@CurrentUser()` 装饰器直接获取 `AuthenticatedUser` 类型对象，避免在业务代码中手动解析 `request.user`。
- DTO 类中广泛使用 `@ApiProperty` 及其变体进行 Swagger 元数据标注，并结合 `class-validator` 装饰器（如 `@IsString`, `@MinLength`）实现声明式参数校验。