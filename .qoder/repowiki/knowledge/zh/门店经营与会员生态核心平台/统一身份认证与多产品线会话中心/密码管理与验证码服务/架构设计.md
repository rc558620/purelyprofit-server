- 核心服务 `AuthPasswordService` 负责密码的哈希处理（bcrypt）、数据库持久化及变更逻辑。
- `AuthCodeService` 协调 Redis 缓存与 `AuthSmsService`，管理注册、登录及找回密码场景下的验证码生命周期（生成、存储、校验、清除）。
- `AuthSmsService` 作为短信发送的基础设施层，当前实现为日志记录，预留了第三方 SMS 提供商的集成接口。
- 类型定义通过 `auth-password.types.ts` 从共享模块重新导出，保持类型契约的一致性。