- 敏感信息脱敏：在构建 Profile 响应时，对身份证号等敏感字段执行掩码处理（如 `maskIdNumber`），仅返回脱敏后的 `idNumberMasked`。
- 条件字段注入：DTO 组装时广泛使用展开运算符配合三元表达式或逻辑判断，仅在字段存在或满足特定身份类型（如 `sub_account`）时才注入对应属性。
- 多源身份解析：在查找用户时，优先匹配 `staff` 表中的活跃记录，若未命中则回退至通用 `user` 表，并关联最新的手机号或邮箱标识。