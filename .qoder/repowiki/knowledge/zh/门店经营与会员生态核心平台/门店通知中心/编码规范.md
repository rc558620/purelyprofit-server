- 通知 ID 采用 `业务域:子类型:业务ID` 的字符串格式（如 `inventory:product:5`）以实现跨业务线的唯一标识。
- 所有通知生成逻辑均在内存中完成，通过 `Promise.all` 并行拉取各业务线数据后合并排序，未引入独立的通知持久化表。
- DTO 转换遵循显式映射原则，通过 `toNotificationItemDto` 等纯函数将内部 Draft 对象转换为带 Swagger 装饰器的响应 DTO。