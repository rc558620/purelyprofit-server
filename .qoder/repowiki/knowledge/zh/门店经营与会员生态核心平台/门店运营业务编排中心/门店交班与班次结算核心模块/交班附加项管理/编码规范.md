- 所有写操作（增删改）前必须通过 `ensureMembershipStoreId` 或 `ensureMembershipContext` 校验用户所属门店及权限。
- 附加项名称在存储前需经过 `normalizeRequiredText` 处理，执行 trim 并截断至最大长度（20字符），且在同一门店内保持大小写不敏感的唯一性。
- 列表查询时自动关联最近一次已完成交班的附加项数值（`findLatestValuesByItemId`），用于前端自动回填。
- 统一使用 `mapAdditionalItem` 等映射器将数据库实体转换为 DTO，确保时间字段统一转换为毫秒级时间戳。