- 所有写操作接口均通过 `ensureMembershipContext` 校验用户门店权限，并根据角色（Owner/Manager/Cashier）执行细粒度的业务规则拦截。
- DTO 定义统一使用 `class-validator` 装饰器进行类型与格式约束，并结合 `ApiProperty` 生成 Swagger 文档。
- 复杂查询逻辑封装在独立的 Service 或 Shared 工具函数中（如 `buildSaleOrderWhere`），保持主服务方法的简洁性与可读性。