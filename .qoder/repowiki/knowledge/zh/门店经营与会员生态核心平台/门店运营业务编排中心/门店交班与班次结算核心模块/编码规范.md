- 统一使用 ensureMembershipContext 和 ensureMembershipStoreId 从 JWT 用户信息中提取门店上下文，确保数据隔离。
- 采用 Decimal.js 封装的 addMoney、subMoney 等工具函数处理所有金额运算，避免浮点数精度丢失。
- 服务层方法统一接收 AuthenticatedUser 作为第一个参数，并在内部立即执行权限与上下文校验。
- DTO 映射逻辑集中在 handover.mapper.ts 或通过 mapRecordToDto 等共享函数处理，保持 API 响应结构一致。