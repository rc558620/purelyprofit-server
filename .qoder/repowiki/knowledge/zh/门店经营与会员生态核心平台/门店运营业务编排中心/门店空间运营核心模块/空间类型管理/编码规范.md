- 写操作（创建、更新、删除）前强制校验当前用户非子账号身份，违反则抛出 ForbiddenException。
- 涉及门店资源的操作均通过 CommerceAccessService 进行跨门店权限边界检查与 ID 解析。
- 空间类型名称在创建和更新时执行同门店下的唯一性校验，防止命名冲突。
- 删除操作前检查关联实体（spaces）计数，若存在引用则阻止删除并抛出 ConflictException。