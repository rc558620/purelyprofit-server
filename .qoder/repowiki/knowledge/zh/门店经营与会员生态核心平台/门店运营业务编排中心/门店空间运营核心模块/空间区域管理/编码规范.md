- 写操作（创建、更新、删除）前强制校验当前用户是否为主账号，子账号直接抛出 ForbiddenException。
- 所有涉及门店 ID 的参数均通过 CommerceAccessService 进行权限解析与校验，确保用户只能操作有权限的门店数据。
- 空间区域名称在存储前执行 trim() 处理，并在创建和更新时检查同名冲突，抛出 ConflictException。
- 删除操作前检查关联数据（spaces 计数），若存在关联记录则阻止删除并抛出 ConflictException，保障数据完整性。