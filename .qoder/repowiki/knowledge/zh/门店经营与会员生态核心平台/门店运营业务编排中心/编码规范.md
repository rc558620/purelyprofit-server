- 所有业务入口服务均以 `AuthenticatedUser` 作为第一个参数，确保操作可追溯至具体员工或账号。
- 跨模块写操作优先通过注入对方模块的顶级 Service（如 `SalesRecordService`）而非直接操作 Repository，以复用其内部的业务校验与缓存失效逻辑。
- 涉及多实体状态变更的操作（如进货同步成本、空间结账生成销售单）均包裹在 `Prisma.$transaction` 中以保证数据一致性。