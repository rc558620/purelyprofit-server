- 所有涉及多表写入或状态联动的操作（如创建进货单并同步成本）均使用 Prisma 事务 (`$transaction`) 保证原子性。
- 数据访问逻辑被抽取至独立的 `*.query.ts` 文件中，服务层仅通过调用这些纯函数或类方法与数据库交互。
- 业务规则与复杂校验逻辑（如供应商名称规范化、金额计算）集中在 `*.domain.ts` 中实现，保持服务层简洁。
- DTO 定义与响应映射逻辑分离，使用专门的 `*.mapper.ts` 文件处理从数据库实体到 API 响应 DTO 的转换。