- 所有涉及金额的 DTO 字段均使用 `@Type(() => Number)` 配合 `IsNumber` 进行严格校验，并在服务层通过 `toDecimalNumber` 统一转换。
- 查询服务（List/Stats/Report）均遵循“权限校验 -> 时间范围构建 -> 数据查询 -> 响应映射”的标准执行流。
- 写操作（创建/删除）完成后均调用 `cacheInvalidatorService.invalidateSalesDerived` 清除相关的缓存数据以保证实时性。