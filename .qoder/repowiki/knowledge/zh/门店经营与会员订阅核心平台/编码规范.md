- 每个业务子域均实现独立的 `*AccessService` 以封装门店维度的权限校验与 ID 解析逻辑。
- 普遍采用 `resolveViewStoreId` 或 `getManageableStoreId` 模式，根据用户角色自动推导或校验目标门店 ID。
- 控制器层统一通过装饰器提取 `AuthenticatedUser`，并将身份对象透传至服务层进行细粒度鉴权。
- 涉及敏感操作时，优先调用 `PlatformMembershipAccessService` 校验当前门店的订阅套餐是否支持该功能。