- 采用 NestJS 模块化架构，将首页（dashboard-home）、业务分析（business-analysis）和利润详情（profit-detail）拆分为独立的功能模块。
- 各子模块通过统一依赖 `CommerceModule` 获取核心交易数据，并依赖 `PlatformMembershipModule` 处理会员与权限逻辑，实现业务解耦。
- 遵循 Controller-Service-Domain 分层模式，各模块内部通过 DTO 定义输入输出契约，确保接口规范性。