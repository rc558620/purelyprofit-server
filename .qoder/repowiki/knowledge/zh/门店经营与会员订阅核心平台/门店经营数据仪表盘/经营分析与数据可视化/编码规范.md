- 采用“领域-映射-查询”分离模式，将数据聚合、DTO 转换和数据库查询逻辑分别封装在 `.domain.ts`、`.mapper.ts` 和 `.query.ts` 文件中。
- 在 DTO 定义中广泛使用 `class-validator` 装饰器（如 `@IsInt`, `@IsNumber`）配合 `class-transformer` 进行请求参数的自动转换与严格校验。
- 使用 TypeScript 映射类型 (`Map`) 在内存中进行高效的数据分组与聚合，避免在循环中频繁进行对象查找或数据库交互。