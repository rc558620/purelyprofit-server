- 使用 `SetMetadata` 定义装饰器以存储权限需求或拦截规则，并在守卫中通过 `Reflector.getAllAndOverride` 读取。
- 权限校验逻辑统一收敛至 `AccessControlService`，支持通配符 `*` 匹配及多权限集合的 `hasAnyPermission` 检查。
- 子账号权限采用白名单机制，在 `SUB_ACCOUNT_ROLE_PERMISSIONS` 中为 cashier/manager/finance 预定义只读权限集合。