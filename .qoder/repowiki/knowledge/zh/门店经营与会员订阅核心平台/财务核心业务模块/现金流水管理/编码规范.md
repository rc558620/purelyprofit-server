- 遵循“查询对象（Query Object）”模式，将复杂的分页、筛选参数封装为独立的 DTO 类，并在 Service 层转换为内部查询输入结构。
- 实行严格的领域规则前置校验，在创建流水前通过 domain 层函数断言分类与方向的一致性，以及是否允许手工录入。
- 统一金额处理规范，所有货币计算均通过 `finance-money.utils` 中的工具函数进行精度控制与格式化，避免浮点数误差。
- 采用“所有权校验”机制，在执行删除或更新等敏感操作前，先通过专用查询方法确认记录归属当前门店，防止越权操作。