- 所有金额字段在入库前通过 `toPrismaDecimal` 转换，业务逻辑中统一使用 `roundMoneyValue` 或 `toMoneyNumber` 进行标准化处理。
- 账款状态（Status）不直接由前端传入，而是根据总金额、已付金额及到期时间在 Domain 层通过 `deriveAccountFields` 自动计算得出。
- 查询接口遵循统一的分页与筛选模式，通过 `buildFinanceAccountWhere` 动态构建 Prisma 查询条件，支持模糊搜索与多状态过滤。