- 采用领域驱动分层：`finance-reconciliation.domain.ts` 负责业务规则（如状态归一化、统计聚合），`finance-reconciliation.query.ts` 封装 Prisma 数据访问，`finance-reconciliation.service.ts` 编排业务流程。
- 通过 `FinanceAccessService` 统一校验门店权限与财务模块开通状态，确保数据隔离。
- 对外由 `FinanceController` 暴露 RESTful 接口，内部通过 `FinanceService` 门面模式路由至各子域服务。
- 使用 DTO 严格定义输入输出契约，并通过 Mapper/Domain 层完成实体到响应对象的转换。