- 所有写操作（创建、确认、删除）完成后，统一调用 `cacheInvalidatorService.invalidateFinanceDerived(storeId)` 清除相关缓存以保持一致性。
- 金额字段在入库前均通过 `roundMoneyValue` 进行精度处理，并转换为 Prisma Decimal 类型，确保财务数据准确性。
- 查询接口普遍采用 `getOrLoadRefreshableJson` 模式，在缓存命中时直接返回，未命中或过期时触发后台刷新或同步加载。
- DTO 定义严格使用 `class-validator` 装饰器（如 `@IsNumber`, `@MaxLength`）进行输入参数校验，并结合 Swagger 注解生成 API 文档。