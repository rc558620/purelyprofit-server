- 金额计算统一使用 `roundMoneyValue` 进行精度处理，并转换为 Prisma Decimal 类型存储。
- 可选字符串字段在入库前统一通过 `trimOptionalString` 进行去空与修剪处理。
- 数据查询逻辑从 Service 层剥离至独立的 `.query.ts` 文件，实现业务编排与数据访问解耦。
- 所有财务操作均通过 `getFinanceStoreIdOrThrow` 强制注入当前用户所属门店 ID，实现多租户数据隔离。