- 所有涉及顾客资产变动（充值、消费）的写操作必须包裹在 `prisma.$transaction` 事务中执行。
- 列表查询接口统一采用 `Promise.all` 并行执行数据行查询与总数统计（count），以优化响应延迟。
- 数据返回前必须经过 `mapXxxRow` 映射器处理，将数据库行对象转换为符合 API 规范的 DTO。
- 在执行任何业务逻辑前，必须先通过 `MarketingSharedService` 完成基于门店 ID 和操作权限（如 'marketing:view'）的鉴权。