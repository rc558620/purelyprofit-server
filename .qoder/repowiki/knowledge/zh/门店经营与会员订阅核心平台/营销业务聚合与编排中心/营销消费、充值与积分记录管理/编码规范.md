- 所有列表查询均采用分页模式，通过 `resolveMarketingPagination` 解析参数，并使用 `Promise.all` 并行执行数据查询与总数统计。
- 写操作完成后，均通过二次查询（如 `queryConsumptionRowById`）获取包含关联信息的完整记录并返回，确保响应数据的一致性。
- 在执行任何业务操作前，必须通过 `MarketingSharedService` 进行门店级别的权限校验（`marketing:view` 或 `marketing:manage`）。