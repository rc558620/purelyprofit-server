- 所有写操作（创建、更新、删除）后均调用 invalidateDashboardCaches 确保仪表盘数据一致性。
- 业务逻辑中统一使用 marketingSharedService.ensureMarketingStoreAccess 进行店铺维度的权限拦截。
- 时间范围校验通过私有方法 assertPromotionRange 强制执行，确保结束时间晚于开始时间。
- 字符串字段在存入数据库前统一执行 trim() 操作以清洗空白字符。