- 金额单位统一采用“分”（integer）进行存储和传输，避免浮点数精度问题。
- 时间字段在 DTO 中统一序列化为毫秒级时间戳（number），而非 ISO 字符串。
- 敏感信息（如手机号）在 Mapper 层通过 `maskPhone` 工具函数进行脱敏处理，确保原始数据不泄露至响应层。
- 空值处理遵循防御性编程，对于无权限或无数据的场景，返回由 `buildEmptyMarketingOverview` 构造的标准空结构而非 null。