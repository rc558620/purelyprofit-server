- 金额与时间标准化：API 响应中金额统一使用“分”为单位的整数，时间统一使用毫秒级时间戳。
- 敏感信息脱敏：顾客手机号在 Mapper 层通过 `maskPhone` 工具函数进行脱敏处理，确保原始数据不泄露至响应层。
- 状态动态计算：顾客等级、活跃状态及活动状态等衍生字段不在数据库存储，而是通过 `marketing.utils.ts` 中的纯函数根据实时数据动态计算。
- ID 字符串化：所有数据库主键 ID 在 DTO 映射时统一转换为字符串类型，以保持前端处理的一致性。