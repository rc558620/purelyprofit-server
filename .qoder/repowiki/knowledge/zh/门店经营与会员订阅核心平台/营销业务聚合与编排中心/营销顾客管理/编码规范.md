- 所有涉及金额的字段的单位统一为“分”（整数），时间戳字段统一为毫秒级整数。
- 顾客手机号在 API 响应中必须进行脱敏处理（如 138****0001），原始号码严禁返回。
- 写操作（创建、更新、删除）成功后必须调用 `cacheInvalidatorService` 清除相关的营销概览缓存。
- 顾客手机号在同一门店内必须保持唯一，写入时需通过 `findUnique` 检查并抛出 `ConflictException`。