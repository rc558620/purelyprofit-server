- 金额与时间戳标准化：API 响应中金额统一使用“分”为单位，时间统一转换为毫秒级时间戳。
- 敏感信息脱敏：顾客手机号在响应 DTO 中通过 `maskPhone` 工具函数进行中间四位脱敏处理。
- 状态动态计算：顾客等级、活跃状态及活动状态不直接存储，而是根据消费金额或时间戳在 Mapper 层动态计算得出。
- ID 字符串化：所有数据库主键 ID 在 DTO 映射时统一转换为字符串类型，以保证前端接收的一致性。