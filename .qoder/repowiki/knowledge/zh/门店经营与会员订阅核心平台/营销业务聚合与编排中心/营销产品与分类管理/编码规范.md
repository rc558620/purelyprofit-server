- 所有写操作（创建、更新、删除、上下架）前均通过 `MarketingSharedService` 执行门店级别的权限校验 (`ensureMarketingStoreAccess`)。
- 在创建或更新产品时，强制校验所属分类是否归属于当前门店 (`ensureCategoryBelongsToStore`)，防止跨门店数据关联。
- 产品分类名称在同一门店内保持唯一性，通过 `ensureUniqueCategoryName` 在创建和更新时进行冲突检测。
- 删除产品分类前必须检查是否存在关联产品，若存在则抛出 `BadRequestException` 阻止删除以维护数据完整性。