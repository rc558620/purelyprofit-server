- 采用“领域-查询-映射”分离模式，将业务规则、数据访问和数据转换逻辑分别封装在 `.domain.ts`、`.query.ts` 和 `.mapper.ts` 文件中。
- 所有写操作（如创建、删除）均通过 `CommerceAccessService` 进行细粒度的门店级权限校验，确保数据隔离。
- 创建进货单时使用 `prisma.$transaction` 保证进货单记录与成本记录的原子性写入。
- DTO 中涉及 ID 或时间戳的字段统一使用 `Transform` 装饰器进行类型安全转换，并配合 `IsInt` 等装饰器进行严格校验。