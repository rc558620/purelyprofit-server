- 所有写操作（创建/更新/删除）前均通过 `CommerceAccessService` 校验当前用户对指定门店的操作权限。
- 供应商名称在门店维度保持唯一，写入时通过 `ensureSupplierNameUnique` 方法进行冲突检测并抛出 `ConflictException`。
- 响应数据中的可选字段（如联系人、电话）若为空则在 DTO 中省略，时间戳统一转换为毫秒级数字格式。
- Controller 层统一注入 `CurrentUser` 和权限守卫，将用户上下文透传至 Service 层进行业务逻辑处理。