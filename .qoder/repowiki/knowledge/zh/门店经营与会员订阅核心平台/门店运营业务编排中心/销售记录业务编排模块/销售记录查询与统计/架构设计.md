- 采用门面模式，`SalesRecordReadService` 作为统一入口，将请求分发给 `SalesRecordListService`（列表）、`SalesRecordProductsService`（商品）、`SalesRecordStatsService`（统计）和 `SalesRecordReportService`（报表）。
- 依赖 `PrismaService` 进行数据持久化访问，并通过 `sales-record.query.ts` 封装底层查询逻辑。
- 利用 `sales-record-read.utils.ts` 处理时间范围解析、空值响应构建等通用逻辑，确保查询参数的一致性。
- 模块通过 `SalesRecordModule` 注册，并与 `CommerceModule`、`PlatformMembershipModule` 等协作以实现门店权限校验和历史数据范围限制。