- 所有查询服务方法均接收 `AuthenticatedUser` 对象，并在业务逻辑起始处调用 `CommerceAccessService` 进行门店维度的权限校验。
- 针对子账号（sub_account）等特殊身份，统一通过 `PlatformMembershipAccessService.clampHistoryRange` 限制可查询的历史数据时间范围。
- 在查询结果为空或无权限时，优先返回由 `buildEmpty...` 工具函数生成的标准化空响应结构，而非直接抛出异常。