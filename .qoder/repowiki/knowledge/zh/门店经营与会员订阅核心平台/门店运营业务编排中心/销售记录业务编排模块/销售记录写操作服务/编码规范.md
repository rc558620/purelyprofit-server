- 采用“准备-校验-执行”模式，将数据预处理（如商品快照提取）与核心业务逻辑（如事务写入）解耦到不同服务类中。
- 在创建销售记录时，强制校验前端传入的汇总值（总额、总利润、总件数）与后端根据明细计算的结果是否一致。
- 支持“手动录单”场景，允许非商品库条目通过传递自定义字符串 ID 并记录名称/分类快照的方式创建销售明细。
- 删除销售记录时采用事务性回滚机制，同步恢复库存扣减并清理关联的现金流记录，确保数据一致性。