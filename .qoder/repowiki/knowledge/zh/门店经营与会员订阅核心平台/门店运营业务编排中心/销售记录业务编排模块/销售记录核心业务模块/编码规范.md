- 所有涉及金额的字段在 DTO 中使用 `number` 类型，但在服务层计算和数据库存储时使用 `Decimal` 或 `Prisma.Decimal` 以确保精度。
- 写操作服务（如创建、删除）在执行完数据库事务后，统一调用 `cacheInvalidatorService` 清除相关的门店缓存。
- 查询服务通过 `CommerceAccessService` 解析门店 ID 并进行权限校验，确保数据访问的安全性与多租户隔离。
- 报表与统计数据生成时，优先从 Redis 缓存读取，并在缓存过期前触发异步刷新任务以平衡性能与实时性。