- 所有写操作（创建/删除）均在 `prisma.$transaction` 中执行，以确保销售单、库存变动和现金流记录的原子性。
- 在创建销售记录前，强制校验前端传入的汇总金额（总营收、总利润、总件数）与后端根据明细计算的结果是否一致。
- 支持“手动录单”模式，允许 `productId` 为空或为自定义字符串，此时不关联具体商品库但需记录快照信息。
- 提供兼容旧版前端的路由控制器 `SalesOrdersCompatController`，通过特定参数（如 `assignToCurrentShiftOperator`）适配不同客户端的业务逻辑。