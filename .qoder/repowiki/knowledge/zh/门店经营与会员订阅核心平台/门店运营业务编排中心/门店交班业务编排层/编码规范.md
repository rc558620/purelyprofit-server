- 所有对外 API 均通过顶层 `HandoverService` 进行路由，禁止 Controller 直接注入子域 Service。
- 写操作接口在执行前必须调用 `ensureUserCanOperateShift` 或 `ensureUserCanManageAdditionalItems` 进行二次权限校验。
- 涉及数据展示的读接口（如列表、详情）统一前置调用 `autoCheckoutCurrentStoreSessions` 以确保营收数据实时性。