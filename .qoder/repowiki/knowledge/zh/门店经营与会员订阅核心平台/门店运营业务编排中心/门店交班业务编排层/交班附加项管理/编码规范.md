- 所有附加项名称在存储前必须经过 `normalizeRequiredText` 处理，执行 trim 并截断至 20 字符上限。
- 附加项名称在同一门店内必须唯一，校验时采用不区分大小写（insensitive）的模式进行数据库查询。
- 列表查询时自动关联最近一次已完成交班记录中的对应值，用于前端自动回填，提升交班效率。
- 删除或更新附加项前，必须先通过 `findAdditionalItemOrThrow` 校验该附加项是否存在且归属于当前用户所在的门店。