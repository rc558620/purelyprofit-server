- 采用 NestJS 标准分层：Controller 处理 HTTP 请求与 Swagger 文档，Service 封装业务逻辑与 Prisma 数据访问。
- 依赖 `CommerceAccessService` 进行门店维度的权限解析与校验，确保操作隔离。
- DTO 模块化设计：通过 `space-zone.dto.ts` 统一导出查询、请求与响应定义，保持接口契约清晰。
- 写操作（创建、更新、删除）在 Service 层强制校验 `sub_account` 身份并抛出 `ForbiddenException`，实现主账号独占控制。