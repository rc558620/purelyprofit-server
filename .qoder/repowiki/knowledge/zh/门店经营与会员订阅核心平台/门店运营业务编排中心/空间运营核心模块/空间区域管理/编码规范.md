- 写操作（创建、更新、删除）前必须调用 `ensurePrimaryAccountOnly` 拦截子账号请求。
- 空间区域名称在持久化前需执行 `trim()` 处理，并在同门店范围内校验唯一性。
- 响应 DTO 中的时间字段统一通过 `toTimestampMs` 转换为毫秒级时间戳。
- 涉及门店关联的 DTO 字段均使用 `transformOptionalInt` 进行类型转换与校验。