- 采用 CQRS 思想拆分读写服务，约定使用 `-read.service.ts` 和 `-write.service.ts` 后缀区分职责。
- 跨服务共享的业务校验、数据转换及常量定义统一存放于 `.shared.ts` 文件中。
- 所有涉及金额计算的字段均强制保留两位小数精度，并通过统一的正则表达式进行校验。
- 控制器层统一注入 `UserWithRequestId` 以支持全链路追踪，并配合 `RequirePermissions` 装饰器实施细粒度权限控制。