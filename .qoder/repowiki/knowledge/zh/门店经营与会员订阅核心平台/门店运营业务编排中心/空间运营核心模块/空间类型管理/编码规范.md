- 写操作（创建、更新、删除）在 Service 层强制校验主账号身份，子账号直接抛出 ForbiddenException。
- 所有涉及门店的操作均通过 CommerceAccessService 解析或校验 storeId，确保数据访问不越权。
- 空间类型名称在存储前执行 trim() 处理，并在同一门店范围内进行唯一性冲突检测。
- 响应数据中的 ID 统一转换为字符串类型，时间字段统一转换为毫秒级时间戳，保持前端消费格式一致。