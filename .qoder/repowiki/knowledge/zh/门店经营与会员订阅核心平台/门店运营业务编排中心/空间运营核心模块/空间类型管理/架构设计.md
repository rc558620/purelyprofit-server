- 采用 NestJS 标准的 Controller-Service 分层架构，`space-types.controller.ts` 处理 HTTP 请求与 Swagger 文档注解，`space-types.service.ts` 封装核心业务逻辑。
- 依赖 `PrismaService` 进行数据库交互，通过 `CommerceAccessService` 实现门店维度的权限隔离与解析。
- DTO 层细分为查询（query）、请求（request）和响应（response）三类，并通过 `space-type.dto.ts` 统一导出，保持接口定义的清晰与模块化。