- 采用 NestJS 标准分层架构：Controller 层处理 HTTP 请求与 Swagger 文档映射，Service 层封装业务逻辑与 Prisma 数据访问。
- 集成 JWT 认证与 RBAC 权限守卫，并在 Service 层实施主账号独占写操作策略（子账号禁止维护配置）。
- 通过 DTO 模块实现请求参数校验（class-validator）与响应数据结构化，确保接口契约清晰。