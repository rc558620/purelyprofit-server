- 写操作服务方法强制校验主账号身份（`ensurePrimaryAccountOnly`），禁止子账号修改空间配置。
- 空间名称在同一门店内必须唯一，通过 `ensureSpaceNameUnique` 在写入前进行冲突检测。
- 使用 `sortOrder` 字段维护空间显示顺序，插入或删除时通过事务自动调整相邻记录的排序值以保持连续性。
- DTO 定义遵循 Swagger 装饰器规范，明确区分 Request DTO（含校验逻辑）与 Response DTO（含嵌套对象类型声明）。