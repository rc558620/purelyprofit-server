- 所有涉及预约时间变更的操作（创建、更新）均执行严格的时间窗口校验（最多预约未来 2 天）及结束时间晚于开始时间的逻辑检查。
- 预约冲突检测仅针对状态为 `pending` 且未过时的记录进行，过时预约自动排除在冲突计算之外。
- 写操作（创建、更新、取消）均包裹在 `prisma.$transaction` 中，并在事务内同步更新关联空间的状态，保证数据一致性。
- DTO 中的时间戳字段统一使用毫秒级整数表示，并通过 `transformOptionalInt` 装饰器进行类型转换。