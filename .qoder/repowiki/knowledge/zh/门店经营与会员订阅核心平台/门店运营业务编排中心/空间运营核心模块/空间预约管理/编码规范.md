- 所有写操作（创建、更新、取消）均包裹在 `prisma.$transaction` 中，确保预约记录变更与空间状态同步的原子性。
- 预约时间字段在 DTO 层定义为毫秒级时间戳（number），并在 Service 层转换为 Date 对象存入数据库。
- 查询接口默认仅返回 `pending` 状态的预约，前端需显式传入 `status` 参数以获取已履约或已取消的记录。
- 在创建或更新预约前，必须调用 `findReservationTimeConflict` 检查同一空间下是否存在时间重叠的待处理预约。