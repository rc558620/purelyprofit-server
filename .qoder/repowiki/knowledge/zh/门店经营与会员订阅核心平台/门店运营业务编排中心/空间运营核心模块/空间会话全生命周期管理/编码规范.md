- 所有写操作 Service 均通过 `CommerceAccessService` 进行门店维度的权限校验，确保数据隔离。
- 涉及状态变更的核心业务（如开台、结账、换房）均包裹在 `prisma.$transaction` 中以保证数据一致性。
- 使用 `.shared.ts` 后缀的文件存放跨服务复用的纯函数逻辑，如 payload 规范化、结算公式计算等。
- DTO 定义集中在 `dto/` 目录下，并按功能前缀（如 `space-session-open.request.dto.ts`）进行细分，保持接口契约清晰。