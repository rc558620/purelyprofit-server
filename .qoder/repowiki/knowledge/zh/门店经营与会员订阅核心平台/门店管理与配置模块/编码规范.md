- 读写服务分离：将数据查询逻辑封装在 `*ReadService`，数据变更逻辑封装在 `*WriteService`。
- 元数据规范化：在 DTO 和 Utils 中统一定义 `normalizeStoreProfileMetadata` 等函数，确保从 Redis 读取或前端传入的数据结构一致且安全。
- 所有权校验：在 Service 层通过 `ensureOwnerOnly` 强制检查用户身份，禁止子账号执行门店核心操作。