- 通过 `AuthModule` 集中编排认证逻辑，导出 `JwtAuthGuard` 和 `JwtModule` 供其他模块复用。
- 采用策略模式（`JwtStrategy`）处理令牌解析，并结合装饰器（如 `@CurrentUser`）在控制器层透明注入用户上下文。
- 内部通过服务拆分（Session, Password, Profile）实现关注点分离，并通过 `forwardRef` 与成员资格模块（`PlatformMembershipModule`）建立跨域依赖以支持复杂的权限校验。