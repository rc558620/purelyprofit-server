- 所有对外暴露的API接口均使用专门的DTO类定义输入输出，并配合@ApiProperty装饰器生成Swagger文档。
- 受保护的资源访问统一使用@UseGuards(JwtAuthGuard)装饰器，并通过@CurrentUser()参数装饰器安全地获取当前登录用户对象。
- 业务逻辑被拆分为多个职责单一的服务类（如AuthPasswordService, AuthSmsService），并在AuthModule中统一注册与管理。