- 所有 DTO 类均使用 `class-validator` 装饰器（如 `@IsString`, `@MinLength`）定义校验规则，并提供中文错误提示。
- 受保护的 API 端点统一使用 `@ApiBearerAuth()` 与 `@UseGuards(JwtAuthGuard)` 组合，并在方法签名中通过 `@CurrentUser()` 获取已认证用户对象。
- API 响应与请求体均通过 Swagger 装饰器（`@ApiOperation`, `@ApiOkResponse` 等）进行元数据标注，以支持自动化文档生成。