- 会话版本控制模式：在 JWT Payload 中嵌入 `sessionVersion`，并在每次密码变更或重置时递增 Redis 中的版本号，使旧令牌自动失效。
- 防御性账号查找：支持通过手机号、自定义账号或管理员别名（`admin`）等多种标识符解析用户身份，并统一映射为内部邮箱格式进行存储与查询。
- 环境敏感型验证码：在非生产环境下，发送短信验证码接口会直接在响应中返回验证码明文，便于自动化测试与调试。
- 原子化业务编排：在注册或登录成功后，并行执行验证码清除、员工成员关系同步（`syncStaffMemberships`）及令牌签发，确保数据最终一致性。