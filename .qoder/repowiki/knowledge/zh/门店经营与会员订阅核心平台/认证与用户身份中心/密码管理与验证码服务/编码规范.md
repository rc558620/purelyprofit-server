- 在非生产环境（nodeEnv !== 'production'）下，验证码接口会直接返回生成的验证码明文以方便调试。
- 所有涉及密码更新的操作均先通过 bcrypt 进行哈希处理后再存入数据库，严禁明文存储。
- 验证码校验失败或短信发送异常时，主动清理 Redis 中对应的验证码记录以保证状态一致性。
- 使用统一的工具函数（如 `buildRegisterCodeKey`）构建 Redis Key，确保键名格式在不同服务间保持一致。