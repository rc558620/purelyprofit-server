- 服务类遵循职责分离原则，将数据查找（Lookup）、业务规则（Membership）与对外接口（Profile/Capability）拆分为独立的可注入服务。
- 在构建响应对象时，广泛使用条件展开运算符（Spread Syntax）根据用户身份类型（如 sub_account）动态增减返回字段，保持 API 响应的精简性。
- 涉及敏感信息（如身份证号）的处理均在服务层通过工具函数（如 `maskIdNumber`）完成脱敏，确保底层数据不直接暴露。