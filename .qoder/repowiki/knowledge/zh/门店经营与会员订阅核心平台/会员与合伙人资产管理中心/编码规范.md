- 所有子模块均采用 NestJS 标准的 Module/Controller/Service 三层架构组织代码。
- 业务逻辑层广泛使用独立的 Mapper 类（如 members.mapper.ts）处理领域模型与 DTO 之间的转换。
- 每个子模块均定义了专用的 AccessService 或 SharedService 以封装复杂的权限校验与跨域共享逻辑。
- 接口请求与响应数据结构严格通过 dto 目录下的专用类进行定义与类型约束。