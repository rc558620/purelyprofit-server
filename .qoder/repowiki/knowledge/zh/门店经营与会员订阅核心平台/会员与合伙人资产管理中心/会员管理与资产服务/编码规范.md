- 所有涉及敏感操作的 Service 方法均优先调用 AccessService 进行权限校验与门店 ID 解析。
- DTO 命名遵循 `{Action}{Entity}Dto` 格式，并按功能模块拆分为独立的 DTO 文件。
- 数据库查询逻辑被抽离至 `.query.ts` 或 `.shared.ts` 文件中，保持 Service 层的简洁与可测试性。
- 使用泛型接口和配置对象模式处理结构相似但字段不同的业务逻辑（如积分与纯利豆的调整）。