- 核心写业务逻辑（如会员购买、合伙人审核）均包裹在 `prisma.$transaction` 中以确保数据一致性。
- 领域逻辑与数据映射解耦，复杂的 DTO 转换和状态计算抽取至 `.domain.ts` 文件中的纯函数。
- 采用“快照”模式（Snapshot），将会员规则、子账号权益等动态配置转化为不可变对象进行传递和校验。
- 敏感操作前统一执行身份与权限校验，如 `ensurePlatformMembershipStoreOwner` 和 `ensureOwnerOnly`。