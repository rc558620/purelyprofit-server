- 采用 NestJS 模块化架构，划分为 members、platform-membership、withdrawals 三个独立业务子模块。
- 所有子模块均依赖并导入共享的 AuthModule 以实现统一的身份认证与权限拦截。
- platform-membership 模块通过 exports 向外暴露 PlatformMembershipService 等核心服务，形成跨模块业务能力支撑。
- 各子模块内部遵循 Controller-Service-DTO 分层设计，通过共享的 DTO 和 Domain 对象维持数据契约的一致性。