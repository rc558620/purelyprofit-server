- 统一采用 CQRS 风格的服务拆分，区分 ReadService 与 WriteService（如 categories 模块）。
- 领域逻辑封装在独立的 Domain 类中（如 inventory-stock.domain.ts, products.domain.ts）。
- 数据传输对象（DTO）统一存放于各子模块的 dto 目录下。
- 使用 Mapper 层处理实体与 DTO 之间的转换逻辑。