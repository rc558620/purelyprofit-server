- 采用 NestJS 标准分层架构：Controller 处理 HTTP 请求与 Swagger 文档，Service 编排业务逻辑，Query 层封装 Prisma 数据访问，Domain 层处理核心规则（如分类去重、编号生成），Mapper 层负责 DTO 转换。
- 依赖注入 CommerceModule 与 PlatformMembershipModule 以实现门店数据隔离访问及商品数量配额校验。
- 内部通过 products.types.ts 定义领域模型接口，products.query.ts 集中管理数据库查询逻辑，确保数据访问层的可维护性。