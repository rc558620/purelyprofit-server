- 读写服务通过内部类型（如 `CategoryListQueryInput`）传递参数，隔离 DTO 与数据库查询逻辑。
- 写操作前统一通过 `CommerceAccessService` 校验用户对特定门店的操作权限。
- 使用 `CATEGORY_SELECT` 常量统一定义 Prisma 查询字段，确保返回数据结构一致。
- 更新分类名称时，同步触发关联商品的分类名称更新 (`renameCategoryProducts`) 以维持数据一致性。