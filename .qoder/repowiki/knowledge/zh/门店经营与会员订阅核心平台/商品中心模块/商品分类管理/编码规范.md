- 读写服务分离：查询操作由 ReadService 处理，写操作由 WriteService 处理，通过主 Service 聚合。
- 权限校验前置：在读写服务中均调用 CommerceAccessService 解析或校验门店 ID 及操作权限。
- DTO 与领域模型隔离：通过独立的 Mapper 函数将数据库记录转换为响应 DTO，避免直接暴露实体。
- 名称唯一性约束：在创建和更新分类时，通过查询服务校验同门店下分类名称的唯一性。