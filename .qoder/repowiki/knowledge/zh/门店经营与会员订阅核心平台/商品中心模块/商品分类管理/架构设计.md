- 采用 CQRS 思想，将读写逻辑拆分为 `CategoriesReadService`（查询）和 `CategoriesWriteService`（变更），由 `CategoriesService` 作为统一门面供 Controller 调用。
- 数据访问层通过 `categories.query.ts` 封装 Prisma 操作，实现业务逻辑与数据库查询解耦。
- 使用 `categories.mapper.ts` 统一处理领域模型到 DTO 的转换，确保响应格式一致。
- 依赖 `CommerceAccessService` 进行门店维度的权限校验，确保多租户数据隔离。
- 入口为 `CategoriesController`，集成 JWT 鉴权与 RBAC 权限守卫。