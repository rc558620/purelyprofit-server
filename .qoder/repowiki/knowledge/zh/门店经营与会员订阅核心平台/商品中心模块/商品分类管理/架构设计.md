- 采用 CQRS 思想分离读写逻辑，`CategoriesReadService` 处理查询，`CategoriesWriteService` 处理变更。
- `CategoriesService` 作为门面层协调读写服务，`CategoriesController` 负责 HTTP 入口与权限守卫。
- `categories.query.ts` 封装 Prisma 原子操作，`categories.mapper.ts` 负责领域模型到 DTO 的转换。
- 依赖 `CommerceModule` 进行门店维度的权限校验 (`CommerceAccessService`)。