# 餐饮账号交班权限控制变更清单

## 🎯 目标
将餐饮类型账号的子账号收银员视角从：
- **旧**: additional + space-management + handover-management
- **新**: additional + scan-ordering + handover-management

---

## ✅ 改动清单（按顺序执行）

### 🔧 Part 1: 后端 purelyprofit-server

#### ✅ Step 1: 修改 CASHIER_SUB_ACCOUNT_PERMISSIONS
**文件路径**: `src/purely-profit/access-control/access-control.service.ts`

**当前内容** (Line 50-61):
```typescript
const CASHIER_SUB_ACCOUNT_PERMISSIONS = [
  'space:view',
  'space:create',
  'space:update',
  'operation-entry:view',
  'operation-entry:create',
  'goods:view',
  'marketing:view',
  'handover:view',
  'handover:create',
  'handover:update',
] as const;
```

**修改为**:
```typescript
const CASHIER_SUB_ACCOUNT_PERMISSIONS = [
  'operation-entry:view',
  'operation-entry:create',
  'goods:view',
  'handover:view',
  'handover:create',
  'handover:update',
] as const;
```

**理由**:
- 移除 `space:view`, `space:create`, `space:update` - 餐饮收银员不可见空间管理
- 移除 `marketing:view` - 避免餐饮收银员获得营销权限
- 保留 operation-entry, goods, handover 相关权限

**影响范围**:
- 餐饮门店的收银员子账号自动失去空间和营销权限
- 不影响 manager 主账号（通过 StaffRole.manager 控制）
- 不影响 owner 主账号（通过 subjectType === 'owner' 控制）

---

### 🎨 Part 2: 前端 purelyprofit

#### ✅ Step 2: 验证 gridCards.ts 的业态控制
**文件路径**: `src/pages/main/dashboard/home/components/FunctionGrid/gridCards.ts`

**检查项** (Line 73-92):
```typescript
// Space Management - 应该是正确的
{
  icon: chairIcon,
  title: '空间管理',
  route: '/space-management',
  subAccountAccess: (accessState) => accessState.canAccessSpaceManagement,
  businessModeAccess: (cap) => cap.canUseSpaceManagement,  // ✅ 已正确控制
  hasCornerDecor: true,
},

// Scan Ordering - 应该是正确的
{
  icon: scanOrderingIcon,
  title: '扫码点餐',
  route: '/scan-ordering',
  subAccountAccess: (accessState) => accessState.canAccessScanOrdering,
  businessModeAccess: (cap) => cap.canUseScanOrdering,  // ✅ 已正确控制
  hasCornerDecor: true,
},
```

**结论**: **无需修改**，前端已有正确的业态控制逻辑

---

### ✅ Part 3: 验证和测试

#### ✅ Step 3: 运行后端测试
```bash
cd /Users/f0rest/Mac/project/React/purelyprofit-server
pnpm test -- access-control.service.spec.ts
```

**预期结果**:
- 餐饮门店收银员的 permissions 不包含 `space:*` 和 `marketing:view`
- 非餐饮门店收银员的 permissions 保持不变

#### ✅ Step 4: 运行前端测试
```bash
cd /Users/f0rest/Mac/project/React/purelyProfit
pnpm test -- FunctionGrid.test.tsx
```

**预期结果**:
- 餐饮门店收银员看到：开始营业、扫码点餐、销售记录、员工交班
- 餐饮门店收银员看不到：空间管理、营销中心
- 非餐饮门店收银员保持原有视图

---

## 🔍 验证 Checklist

### ✅ 后端验证
- [ ] 餐饮门店创建收银员子账号后，登录获取 capability
- [ ] 检查返回的 `permissions` 数组不包含 `space:*` 和 `marketing:view`
- [ ] 检查返回的 `capability.canUseScanOrdering` 为 `true`
- [ ] 检查返回的 `capability.canUseSpaceManagement` 为 `false`

### ✅ 前端验证
- [ ] 餐饮门店登录后，首页 FunctionGrid 显示功能卡片
- [ ] 确认有"扫码点餐"入口（橙色图标）
- [ ] 确认无"空间管理"入口（琥珀色图标）
- [ ] 确认无"营销中心"入口（紫色图标）
- [ ] 确认有"开始营业"入口（ lime 绿色）
- [ ] 确认有"员工交班"入口（翠绿色）

### ✅ 路由守卫验证
- [ ] 尝试直接访问 `/space-management` → 应被守卫拦截
- [ ] 尝试直接访问 `/scan-ordering` → 应成功进入

---

## 📊 最终效果对比

| 账号类型 | 门店业态 | visible cards | hidden cards |
|---------|---------|--------------|--------------|
| 收银员 | catering | 开始营业、扫码点餐、销售记录、商品管理、库存盘点、成本管理、利润详情、员工交班 | 空间管理、营销中心、进货管理、报表中心、会员管理、财务管理 |
| 收银员 | general | 开始营业、空间管理、销售记录、商品管理、库存盘点、成本管理、利润详情、员工交班 | 扫码点餐、营销中心、进货管理、报表中心、会员管理、财务管理 |
| owner | catering/general | 全部可见 | 无 |
| manager | catering/general | 全部可见 | 无 |

---

## 🔄 回滚方案

如果出现问题，可通过以下 git 命令回滚：

```bash
# 查看修改
git diff src/purely-profit/access-control/access-control.service.ts

# 撤销修改
git checkout src/purely-profit/access-control/access-control.service.ts
```

---

## 📝 备注

1. **权限模型原则**：
   - 子账号的入口由 **权限码 + 业态能力** 双重控制
   - 业态能力是前置过滤条件，即使有权限码也会被隐藏
   - 移除权限码可减少不必要的权限污染

2. **安全性考虑**：
   - 后端权限过滤是首要防线
   - 前端入口隐藏是体验优化
   - 路由守卫是最后防线

3. **扩展性**：
   - 新增业态时，只需调整 `StoreBusinessCapabilityService`
   - 子账号权限会自动继承业态限制
